<?xml version="1.0" encoding="utf-8"?>

<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#F6FAFF"
    android:padding="16dp">

    <!-- Pantalla de resultado -->
    <TextView
        android:id="@+id/txtResultado"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="0"
        android:textSize="36sp"
        android:textColor="#6B7C93"
        android:gravity="end"
        android:padding="20dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <!-- Botones -->
    <GridLayout
        android:id="@+id/gridLayout"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:columnCount="4"
        android:useDefaultMargins="true"
        android:alignmentMode="alignMargins"
        app:layout_constraintTop_toBottomOf="@id/txtResultado"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <!-- Fila 1 -->
        <Button android:text="7" style="@style/BtnWhite"/>
        <Button android:text="8" style="@style/BtnWhite"/>
        <Button android:text="9" style="@style/BtnWhite"/>
        <Button android:text="/" style="@style/BtnWhite"/>

        <!-- Fila 2 -->
        <Button android:text="4" style="@style/BtnWhite"/>
        <Button android:text="5" style="@style/BtnWhite"/>
        <Button android:text="6" style="@style/BtnWhite"/>
        <Button android:text="*" style="@style/BtnWhite"/>

        <!-- Fila 3 -->
        <Button android:text="1" style="@style/BtnWhite"/>
        <Button android:text="2" style="@style/BtnWhite"/>
        <Button android:text="3" style="@style/BtnWhite"/>
        <Button android:text="-" style="@style/BtnWhite"/>

        <!-- Fila 4 -->
        <Button android:text="0" style="@style/BtnWhite"/>
        <Button android:text="." style="@style/BtnWhite"/>
        <Button android:text="=" style="@style/BtnEqual"/>
        <Button android:text="+" style="@style/BtnWhite"/>

    </GridLayout>

</androidx.constraintlayout.widget.ConstraintLayout>